sphinx<=3.5 #due to 'env-purge-doc' threw an exception (exception: cannot unpack non-iterable bool object) raised
m2r2>=0.2.7 # m2r is not maintained anymore, this fork is
pybtex>=0.22.2
sphinx_rtd_theme
matplotlib
numpy # required by cartopy
obspy
cartopy

# deps used in tutorial but not working anymore
# basemap
# mlpy
